export const AI_FEATURES = {
  USE_CACHE: true,
  FALLBACK_ENABLED: true,
  STRUCTURED_OUTPUTS_STRICT_MODE: false,
  ENABLE_TRACING: true,
  MOCK_MODE: false, // Useful for testing without burning API credits
};
